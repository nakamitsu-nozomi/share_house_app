<%# ユーザー情報パート%>
<%= render 'user_info' %>
<%# 投稿物件一覧パート %>
<% if @clips.empty?%>
  <p class="no-item"> 保存した物件はありません。</p>
<% else %>
<%= render :partial => "houses/house_list", :locals => { houses: @clips }  %>
<% end %>
